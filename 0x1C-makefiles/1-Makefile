# get all the C source files in the current directory 
SRC = main.c school.c

# set the C compiler to use
CC = gcc

# create the executable file `school` from the C source files
all:
	${CC} ${SRC} -o school
